# ============================================
# Local environment configuration (example)
# Copy this file to .env and set your own values
# ============================================

# JWT secret (must be at least 32 characters for HS256)
JWT_SECRET=your-local-dev-jwt-secret-minimum-32-characters